(()=>{'use strict';
const $=(s,c=document)=>c.querySelector(s),$$=(s,c=document)=>[...c.querySelectorAll(s)];
const root=document.documentElement,themeButton=$('.theme-toggle');
const savedTheme=localStorage.getItem('grc-theme');
const setTheme=theme=>{root.dataset.theme=theme;root.style.colorScheme=theme;themeButton?.setAttribute('aria-pressed',String(theme==='light'));themeButton?.setAttribute('aria-label',theme==='light'?'Koyu temaya geç':'Açık temaya geç');const icon=$('span',themeButton);if(icon)icon.textContent=theme==='light'?'☾':'☀'};
setTheme(savedTheme==='light'?'light':'dark');
themeButton?.addEventListener('click',()=>{const next=root.dataset.theme==='light'?'dark':'light';localStorage.setItem('grc-theme',next);setTheme(next)});
const nav=$('.nav'),menu=$('.menu');menu?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open)});$$('nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const observer='IntersectionObserver'in window?new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.1}):null;$$('.reveal').forEach(el=>observer?observer.observe(el):el.classList.add('visible'));
const modal=$('#quiz-modal'),quiz=$('.quiz'),form=$('#quiz-form');
if(modal&&quiz&&form){let state={},sequence=[],current=0;const steps=$$('.quiz-step',form),offers={student:{discount:50,title:'Güzel! Öğrenci avantajın %50.',copy:'E-devlet onaylı veya yaşadığın ülkeden resmî öğrenci belgeni getirerek %50 indirimden yararlanabilirsin.'},graduate:{discount:25,title:'Yeni mezun avantajın %25.',copy:'Kariyerinin başında güçlü bir denetim hikâyesi kurman için programa %25 indirimle katılabilirsin.'},working:{discount:15,title:'Kariyer değerlendirme avantajın %15.',copy:'Anketi tamamlayıp programın sana uygun olduğuna karar verirsen %15 indirimden yararlanabilirsin.'}};
  const setField=(name,value)=>{state[name]=value;const input=$(`[name="${name}"]`,form);if(input)input.value=value};
  const common=['goal','opportunity','effort','english','time','ethics','lead'];
  const buildSequence=profile=>profile==='working'?['profile-intro','eligibility','residence']:['profile-intro','eligibility',...common];
  const show=view=>{const index=sequence.indexOf(view);current=index<0?0:index;steps.forEach(s=>s.classList.toggle('active',s.dataset.view===view));const total=sequence.length,pct=Math.round(((current+1)/total)*100);$('#step-label').textContent=`Adım ${current+1} / ${total}`;$('#progress-percent').textContent=`${pct}%`;$('#progress-bar').style.width=`${pct}%`};
  const advance=(name,value)=>{if(name==='residence_type'){if(value==='turkey')sequence=['profile-intro','eligibility','residence','age',...common];else sequence=['profile-intro','eligibility','residence','region',...common]}show(sequence[current+1]||'lead')};
  const saveAssessment=async()=>{const data=new FormData();data.set('csrfmiddlewaretoken',$('[name="csrfmiddlewaretoken"]',form).value);data.set('email',$('[name="email"]',form).value.trim());Object.entries(state).forEach(([key,value])=>data.set(key,value));const response=await fetch(quiz.dataset.assessmentUrl,{method:'POST',body:data,headers:{'X-Requested-With':'XMLHttpRequest'}}),json=await response.json();if(!response.ok)throw json;return json};
  const renderResult=(result,title,reasons,discount)=>{form.hidden=true;$('.quiz-direct-start',quiz).hidden=true;const box=$('.quiz-result',quiz);$('h3',box).textContent=title;$('.result-copy',box).textContent=result==='strong'?'GRC kariyeri profilin, hedefin ve çalışma yaklaşımın için güçlü bir seçenek olabilir.':result==='wait'?'Türkiye’deki mevcut başlangıç koşulların nedeniyle bu yoğun kariyer dönüşüm programı şu an sana uygun görünmüyor.':'GRC kariyeri uygun olabilir; hedef, İngilizce, etik sorumluluk veya zaman planını bilinçli şekilde güçlendir.';$('ul',box).innerHTML=reasons.map(x=>`<li>${x}</li>`).join('');const offer=$('.student-result',box);offer.textContent=discount?`3 gün geçerli kişisel avantajın: %${discount}. İndirim bağlantın için destek@grcustasi.co adresine e-posta gönder; sana özel bağlantı oluşturulup paylaşılacaktır.`:'';offer.hidden=!discount;box.hidden=false};
  $$('.profile-picks button').forEach(btn=>btn.addEventListener('click',()=>{form.reset();$('.quiz-direct-start',quiz).hidden=true;$$('.options button',form).forEach(b=>b.classList.remove('selected'));$('.age-note').hidden=true;$('.english-note').hidden=true;state={existing_it_experience:'false',age_over_45:'false'};setField('profile_type',btn.dataset.profile);setField('age_over_45','false');setField('existing_it_experience','false');sequence=buildSequence(btn.dataset.profile);const offer=offers[btn.dataset.profile];$('.profile-offer-title').textContent=offer.title;$('.profile-offer-copy').textContent=offer.copy;$('.discount-note-title').textContent=`%${offer.discount} kişisel avantaj`;$('.discount-note-copy').textContent='Anketi tamamladıktan sonra 3 gün geçerlidir.';form.hidden=false;$('.quiz-result',quiz).hidden=true;show(sequence[0]);if(!modal.open)modal.showModal();document.body.classList.add('modal-open')}));
  $('.capture-email').addEventListener('click',async()=>{const error=$('.profile-email-error');error.textContent='';const email=$('[name="email"]',form);if(!email.reportValidity())return;const button=$('.capture-email');button.disabled=true;try{await saveAssessment();advance('email',email.value)}catch(json){error.textContent=json.message||'E-posta kaydedilemedi.'}finally{button.disabled=false}});
  $('.modal-close').addEventListener('click',()=>modal.close());modal.addEventListener('click',e=>{if(e.target===modal)modal.close()});modal.addEventListener('close',()=>document.body.classList.remove('modal-open'));$('.modal-program').addEventListener('click',()=>modal.close());
  if(quiz.dataset.autoOpen==='true'){form.hidden=true;$('.quiz-result',quiz).hidden=true;$('.quiz-direct-start',quiz).hidden=false;modal.showModal();document.body.classList.add('modal-open')}
  $$('.options button',form).forEach(btn=>btn.addEventListener('click',async()=>{const name=btn.dataset.name,value=btn.dataset.value;setField(name,value);btn.parentElement.querySelectorAll('button').forEach(b=>b.classList.toggle('selected',b===btn));if(name==='age_over_45'&&value==='true'&&state.profile_type==='working'&&state.residence_type==='turkey'){setField('assessment_completed','true');try{const saved=await saveAssessment();renderResult('wait','Program şu an sana uygun görünmüyor.',['Türkiye’de 45 yaş üzerindeki bu başlangıç profili için programın yoğun kariyer dönüşüm modeli uygun değildir.','Daha kademeli bir teknoloji ve iş süreçleri rotasıyla başlamanı öneriyoruz.'],saved.discount)}catch(json){$('.age-note').hidden=false;$('.age-note').textContent=json.message||'Cevabın kaydedilemedi; lütfen tekrar dene.'}return}saveAssessment().catch(()=>{});let delay=180;if(name==='english_awareness'&&value==='false'){ $('.english-note').hidden=false;delay=700 }setTimeout(()=>advance(name,value),delay)}));
  form.addEventListener('submit',async e=>{e.preventDefault();const error=$('.lead-step .form-error');error.textContent='';const data=new FormData(form);['english_awareness','weekly_time','age_over_45','existing_it_experience','eligibility_awareness','career_clarity','opportunity_awareness','effort_awareness','ethics_commitment'].forEach(k=>{if(!data.has(k))data.set(k,'false')});const submit=$('.submit',form);submit.disabled=true;try{const response=await fetch(quiz.dataset.leadUrl,{method:'POST',body:data,headers:{'X-Requested-With':'XMLHttpRequest'}}),json=await response.json();if(!response.ok)throw json;renderResult(json.result,json.title,json.reasons,json.discount)}catch(json){error.textContent=json.message||Object.values(json.errors||{}).flat().join(' ')||'Bir hata oluştu.';submit.disabled=false}});
}
const auditMapRoot=$('[data-audit-map]');
if(auditMapRoot){
  const auditLocations=[
    {id:'los-angeles',name:'Los Angeles',flag:'🇺🇸',x:185,y:160,detail:'2. Seviye Kontrol',labelX:9,labelY:-8},
    {id:'new-york',name:'New York',flag:'🇺🇸',x:294,y:137,detail:'Bankacılık Denetimi',labelX:9,labelY:-8},
    {id:'rotterdam',name:'Rotterdam',flag:'🇳🇱',x:510,y:96,detail:'Denetim',labelX:-10,labelY:-10,anchor:'end'},
    {id:'brussels',name:'Brüksel',flag:'🇧🇪',x:514,y:111,detail:'Bankacılık Denetimi • Credendo ve KBC',labelX:-10,labelY:17,anchor:'end'},
    {id:'paris',name:'Paris',flag:'🇫🇷',x:499,y:126,detail:'Denetim',labelX:-10,labelY:18,anchor:'end'},
    {id:'prague',name:'Prag',flag:'🇨🇿',x:545,y:103,detail:'Denetim',labelX:10,labelY:-8},
    {id:'varna',name:'Varna',flag:'🇧🇬',x:566,y:124,detail:'Denetim',labelX:10,labelY:17},
    {id:'manisa',name:'Manisa',flag:'🇹🇷',x:578,y:141,detail:'OSB ve KOBİ Denetimi',turkeyCity:true,labelX:-11,labelY:-10,anchor:'end'},
    {id:'izmir',name:'İzmir',flag:'🇹🇷',x:574,y:148,detail:'OSB ve KOBİ Denetimi',turkeyCity:true,labelX:-11,labelY:19,anchor:'end'},
    {id:'istanbul',name:'İstanbul',flag:'🇹🇷',x:590,y:136,detail:'QNB Finansbank ve Garanti BBVA • Bankacılık Denetimi',turkeyCity:true,labelX:11,labelY:-9},
    {id:'ankara',name:'Ankara',flag:'🇹🇷',x:601,y:145,detail:'Savunma Sanayii • Denetim',turkeyCity:true,labelX:11,labelY:18},
    {id:'kahramanmaras',name:'Kahramanmaraş',flag:'🇹🇷',x:615,y:154,detail:'OSB ve KOBİ Denetimi',turkeyCity:true,labelX:11,labelY:22},
    {id:'singapore',name:'Singapur',flag:'🇸🇬',x:788,y:246,detail:'Denetim ve 2. Seviye Kontrol'},
    {id:'hong-kong',name:'Hong Kong',flag:'🇭🇰',x:817,y:188,detail:'Bankacılık Denetimi'},
    {id:'shanghai',name:'Şanghay',flag:'🇨🇳',x:837,y:163,detail:'Bankacılık ve KOBİ Denetimi'},
    {id:'tokyo',name:'Tokyo',flag:'🇯🇵',x:888,y:151,detail:'Bankacılık ve KOBİ Denetimi'},
    {id:'ho-chi-minh',name:'Ho Chi Minh Kenti',flag:'🇻🇳',x:796,y:220,detail:'Bankacılık ve KOBİ Denetimi'}
  ];
  const svgNS='http://www.w3.org/2000/svg',nodesLayer=$('.audit-nodes',auditMapRoot),tooltip=$('.audit-tooltip',auditMapRoot),mapShell=$('.audit-map-shell',auditMapRoot);
  const showAuditTooltip=(item,node)=>{const shellRect=mapShell.getBoundingClientRect(),nodeRect=node.getBoundingClientRect();tooltip.innerHTML=`<b>${item.name} ${item.flag}</b><span>ŞEHİR VE SEKTÖR DENEYİMİ</span><p>${item.detail}</p>`;tooltip.hidden=false;const left=Math.min(shellRect.width-230,Math.max(10,nodeRect.left-shellRect.left+12)),top=Math.max(10,nodeRect.top-shellRect.top-92);tooltip.style.left=`${left}px`;tooltip.style.top=`${top}px`;$$('.audit-node',auditMapRoot).forEach(el=>el.classList.toggle('active',el===node))};
  const hideAuditTooltip=()=>{tooltip.hidden=true;$$('.audit-node',auditMapRoot).forEach(el=>el.classList.remove('active'))};
  const buildAuditMap=()=>{
    auditLocations.forEach((item,index)=>{const group=document.createElementNS(svgNS,'g');group.setAttribute('class',`audit-node${item.turkeyCity?' turkey':''}`);group.setAttribute('transform',`translate(${item.x} ${item.y})`);group.setAttribute('tabindex','0');group.setAttribute('role','button');group.setAttribute('aria-label',`${item.name}, profesyonel denetim deneyimi`);group.style.setProperty('--delay',`${.12+index*.09}s`);group.innerHTML=`<circle class="node-hit" r="20"/><circle class="node-ring" r="7"/><circle class="node-core" r="3.6"/><text x="${item.labelX??9}" y="${item.labelY??-8}" text-anchor="${item.anchor??'start'}">${item.name}</text>`;group.addEventListener('mouseenter',()=>showAuditTooltip(item,group));group.addEventListener('mouseleave',hideAuditTooltip);group.addEventListener('focus',()=>showAuditTooltip(item,group));group.addEventListener('blur',hideAuditTooltip);group.addEventListener('click',event=>{event.stopPropagation();showAuditTooltip(item,group)});nodesLayer.append(group)});
    mapShell.addEventListener('click',event=>{if(!event.target.closest('.audit-node'))hideAuditTooltip()});
  };
  if('IntersectionObserver'in window){const mapObserver=new IntersectionObserver(entries=>{if(entries.some(entry=>entry.isIntersecting)){buildAuditMap();mapObserver.disconnect()}},{threshold:.18});mapObserver.observe(auditMapRoot)}else buildAuditMap();
}
const readiness=$('.payment-readiness'),paymentAction=$('.payment-action');
if(readiness&&paymentAction){const checks=$$('input[type="checkbox"]',readiness),sync=()=>{const ready=checks.every(input=>input.checked);paymentAction.classList.toggle('ready',ready);paymentAction.setAttribute('aria-disabled',String(!ready))};checks.forEach(input=>input.addEventListener('change',sync));paymentAction.addEventListener('click',event=>{if(paymentAction.getAttribute('aria-disabled')==='true')event.preventDefault()});sync()}
})();
